name: Publish Package to npmjs

on:
  push:
    tags: ['v*']

jobs:
  build-x86_64:
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm base-devel pkg-config nodejs npm rustup
          rustup default stable
          npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --no-optional

      - name: Build
        run: pnpm run build

      - name: Test
        run: pnpm test

      - name: Upload native binding
        uses: actions/upload-artifact@v4
        with:
          name: bindings-linux-x64-gnu
          path: "*.node"
          if-no-files-found: error

      - name: Upload JS binding
        uses: actions/upload-artifact@v4
        with:
          name: js-binding
          path: |
            index.js
            index.d.ts
          if-no-files-found: error

  build-arm:
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: aarch64
            artifact_name: bindings-linux-arm64-gnu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build (${{ matrix.arch }})
        uses: uraimo/run-on-arch-action@v3
        with:
          arch: ${{ matrix.arch }}
          distro: archarm_latest
          githubToken: ${{ github.token }}
          install: |
            sed -i '/^\[options\]/a DisableSandbox' /etc/pacman.conf
            pacman -Syu --noconfirm
            pacman -S --noconfirm base-devel pkg-config nodejs npm rustup
            rustup default stable
            npm install -g pnpm
          run: |
            pnpm install --no-optional
            pnpm run build

      - name: Upload native binding
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: "*.node"
          if-no-files-found: error

  publish:
    needs: [build-x86_64, build-arm]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "25.x"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm install -g pnpm && pnpm install --no-optional

      - name: Set version from tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "Publishing version $VERSION"
          npm version "$VERSION" --no-git-tag-version --allow-same-version
          for dir in npm/*/; do
            (cd "$dir" && npm version "$VERSION" --no-git-tag-version --allow-same-version)
          done
          npm pkg set optionalDependencies.libalpm-linux-x64-gnu="$VERSION"
          npm pkg set optionalDependencies.libalpm-linux-arm64-gnu="$VERSION"

      - name: Download JS binding
        uses: actions/download-artifact@v4
        with:
          name: js-binding
          path: .

      - name: Download all native bindings
        uses: actions/download-artifact@v4
        with:
          pattern: bindings-*
          path: artifacts
          merge-multiple: true

      - name: Create npm dirs and move artifacts
        run: |
          npx napi create-npm-dirs
          npx napi artifacts --npm-dir npm --build-output-dir artifacts

      - name: List packages
        run: |
          echo "=== Main package ==="
          npm pack --dry-run
          for dir in npm/*/; do
            echo "=== $dir ==="
            (cd "$dir" && npm pack --dry-run)
          done

      - name: Publish platform packages
        run: |
          for dir in npm/*/; do
            if [ -d "$dir" ] && ls "$dir"*.node 1>/dev/null 2>&1; then
              echo "Publishing $dir..."
              (cd "$dir" && npm publish --provenance --access public)
            fi
          done

      - name: Publish main package
        run: npm publish --provenance --access public

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" --generate-notes
